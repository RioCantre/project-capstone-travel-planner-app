import { postData, generateForm, reformatDate } from '../src/client/js/app.js';

describe("Testing the submit functionality", () => {
    test("Testing the postData() function", () => {
        expect(postData).toBeDefined();
    })
});

describe("Testing the submit functionality", () => {
    test("Testing the generateForm() function", () => {
        expect(generateForm).toBeDefined();
    })
});

describe("Testing the submit functionality", () => {
    test("Testing the reformatDate() function", () => {
        expect(reformatDate).toBeDefined();
    })
});


